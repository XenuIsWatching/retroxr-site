---
---

<div class="rxr-conn-grid"><slot /></div>

<style>
	.rxr-conn-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(min(20rem, 100%), 1fr));
		gap: 1.25rem;
		margin-block: 1.75rem;
	}
</style>
